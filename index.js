const writeCards = ["Charlie", "Samip","Ali"];

function writeThankyou(birthday){
    let i = 0;
    while (i < birthday.length){
        console.log (`Thank you, ${writeCards[0]}for the wonderful birthday gift!`);
    
    }
    return writeCards;
}
writeThankyou(writeCards);
